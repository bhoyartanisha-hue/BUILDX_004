import {Camera,ImagePlus,RefreshCw,Trash2} from 'lucide-react';import {useRef,useState,type DragEvent} from 'react';import {Button} from '@/components/ui/button';
export function ImageUpload({label,value,onChange}:{label:string;value?:string|undefined;onChange:(v?:string|undefined)=>void}){const ref=useRef<HTMLInputElement>(null);const[drag,setDrag]=useState(false);const read=(f?:File)=>{if(!f||!f.type.startsWith('image/'))return;const r=new FileReader();r.onload=()=>typeof r.result==='string'&&onChange(r.result);r.readAsDataURL(f)};const drop=(e:DragEvent)=>{e.preventDefault();setDrag(false);read(e.dataTransfer.files[0])};return <section className="space-y-2"><h3 className="text-sm font-semibold">{label}</h3><input ref={ref} className="sr-only" type="file" accept="image/*" capture="environment" onChange={e=>read(e.target.files?.[0])}/>{value?<div className="upload-preview"><img src={value} alt={`${label} preview`} /><div className="upload-actions"><Button type="button" size="sm" variant="secondary" onClick={()=>ref.current?.click()}><RefreshCw/>Replace</Button><Button type="button" size="icon" variant="destructive" aria-label={`Remove ${label}`} onClick={()=>onChange()}><Trash2/></Button></div></div>:<button type="button" className={`upload-drop ${drag?'is-dragging':''}`} onClick={()=>ref.current?.click()} onDragOver={e=>{e.preventDefault();setDrag(true)}} onDragLeave={()=>setDrag(false)} onDrop={drop}><span className="upload-icon"><Camera/></span><strong>Take or add a photo</strong><span><ImagePlus/> Drag and drop on desktop</span></button>}</section>}
