import type { Severity,UtilityType } from './types';
export function classifyComplaint(text:string):{category:UtilityType;department:string;severity:Severity}{const q=text.toLowerCase();if(/sewer|sewage|drain|overflow/.test(q))return{category:'sewer',department:'Water & Sanitation',severity:/overflow|collapse/.test(q)?'critical':'high'};if(/water|leak|pipe/.test(q))return{category:'water',department:'Water Supply',severity:'high'};if(/electric|wire|light|cable/.test(q))return{category:'electricity',department:'Electrical Services',severity:'high'};if(/gas|smell/.test(q))return{category:'gas',department:'Emergency Utilities',severity:'critical'};return{category:'chamber',department:'Public Works Department',severity:'medium'}}
