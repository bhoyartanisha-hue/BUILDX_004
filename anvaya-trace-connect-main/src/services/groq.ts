import type { Language } from "@/data/types";
// One AI integration owns classification, multilingual acknowledgments, and maintenance predictions; seeded responses keep frontend-first demos honest.
export async function classifyComplaint(text:string,category:string){const normalized=`${text} ${category}`.toLowerCase();const water=/water|leak|पानी|पाणी/.test(normalized);const road=/pothole|road|खड्ड|सड़क/.test(normalized);return {category:water?"Water leakage":road?"Pothole":category||"Civic infrastructure",department:water?"Water Works":road?"Public Works":"NMC Command Centre",urgency:/major|danger|continuous|मोठ|खतरा/.test(normalized)?"Immediate":"High",severity:water?86:72};}
export async function generateAcknowledgment(language:Language,id:string){const messages={en:`Report ${id} is now connected to the responsible department. Updates will follow this same tracking ID.`,hi:`रिपोर्ट ${id} अब संबंधित विभाग से जुड़ गई है। सभी अपडेट इसी ट्रैकिंग आईडी पर मिलेंगे।`,mr:`अहवाल ${id} आता संबंधित विभागाशी जोडला आहे. सर्व पुढील माहिती याच ट्रॅकिंग क्रमांकावर मिळेल.`};return messages[language]}
export async function predictMaintenance(context:string){return `Seeded planning signal: ${context}`}
