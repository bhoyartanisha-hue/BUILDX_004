import { ChevronDown } from "lucide-react";
import { useState } from "react";
import type { TimelineStep } from "@/data/types";
const labels={received:"Received",classified:"Classified",routed:"Routed",assigned:"Contractor assigned",progress:"In progress",resolved:"Resolved"};
export function ComplaintTimeline({steps}:{steps:TimelineStep[]}){const [expanded,setExpanded]=useState<string>("progress");return <ol className="relative ml-3 border-l border-chamber/45">{steps.map((step,index)=><li key={step.status} className="relative pb-5 pl-8 last:pb-0"><span className={`absolute -left-[7px] top-1 size-3 border ${step.complete?"border-chamber bg-chamber":"border-graphite bg-paper"}`}/><button type="button" className="flex w-full items-start justify-between gap-3 text-left focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-chamber" onClick={()=>setExpanded(value=>value===step.status?"":step.status)} aria-expanded={expanded===step.status}><span><span className="font-mono text-xs text-muted-foreground">{String(index+1).padStart(2,"0")}</span><span className="ml-3 font-medium">{labels[step.status]}</span></span><ChevronDown className={`size-4 transition-transform ${expanded===step.status?"rotate-180":""}`}/></button><div className={`grid transition-[grid-template-rows,opacity] duration-200 ${expanded===step.status?"grid-rows-[1fr] opacity-100":"grid-rows-[0fr] opacity-0"}`}><div className="overflow-hidden"><div className="mt-2 border-l border-border pl-3 text-sm text-muted-foreground"><p className="font-mono text-xs">{step.at}</p><p className="mt-1">{step.department} · {step.detail}</p></div></div></div></li>)}</ol>}
